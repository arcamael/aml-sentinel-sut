"""Deterministic data generators for AML-Sentinel (doc 04).

All generation is seeded (``SEED=42``) and reproducible: re-running produces
byte-identical output. Phase 3 implements the ``golden`` (normalization) slice;
later phases extend this package with watchlists, profiles, and update scenarios.
"""
